<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jun.tools">

    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity android:name=".filebrower.FileBrowerActivity" />
        <activity android:name=".filebrower.FilePickActivity" />
        <activity android:name=".Activity.MainActivity" />

        <service android:name=".Services.FloatingWindowService" />

        <activity android:name=".Activity.ActivityFloatingWindow" />

        <service android:name=".Location.LocationService"/>

        <!-- TencentLocation Key -->
        <meta-data
            android:name="TencentMapSDK"
            android:value="VAJBZ-N6ZRR-5PTWG-WZTO7-LGYRZ-IKFRX"/>
    </application>



</manifest>